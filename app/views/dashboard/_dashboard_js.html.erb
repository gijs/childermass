<script type="text/javascript">
  $(document).ready(function() {
    App = {};
    App.user = new Childermass.Models.GithubUser({login: "mjansen401"});
    App.repos = new Childermass.Collections.GithubRepos();
    App.currentUserView = new Childermass.Views.CurrentUser({el: '#current_user', model: App.user});
    App.userSelectView = new Childermass.Views.UserSelect({el: '#user_select', model: App.user, collection: App.repos});
    App.reposView = new Childermass.Views.ReposCollection({el: '#repos_table', collection: App.repos});
    App.userSelectView.render();
  });
</script>

